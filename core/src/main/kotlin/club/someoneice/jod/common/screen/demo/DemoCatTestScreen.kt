package club.someoneice.jod.common.screen.demo

/*
class DemoCatTestScreen: BaseScreen() {
    val cat = Cat()
    val inputCtrl = KeyInputHolder()

    override fun join() {
        this.cat.setScale(5.0f)
        this.cat.setPosition(GameBasicInfo.WINDOWS_WIDTH / 2f, GameBasicInfo.WINDOWS_HEIGHT / 2f)

        this.disposeableSet.add(this.cat)
    }

    override fun render(delta: Float) {
        super.render(delta)
        ScreenUtil.initScreen()

        this.batch.begin()
        this.cat.update(delta)
        this.cat.setStateByKeyInput(this.inputCtrl)
        this.cat.draw(this.batch, 1.0f)
        this.batch.end()
    }

    override fun keyDown(keycode: Int): Boolean = inputCtrl.putKey(keycode)
    override fun keyUp(keycode: Int): Boolean = inputCtrl.removeKey(keycode)
}
*/
